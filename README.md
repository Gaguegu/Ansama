# ANSAMA

Aplicación Android del comparador de tarifas eléctricas.

## Compilación automática
El workflow de GitHub Actions genera `app-debug.apk` cuando se hace push a `main` o se ejecuta manualmente.

## Nota
El icono incluido es provisional. Para reproducir exactamente el logo del Excel hace falta incorporar el archivo de imagen/logo original.
